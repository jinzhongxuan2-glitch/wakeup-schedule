# 更新日志

## v1.2.6 — 开下前先测速选源

用户提出「让更新走 VPN 会不会更快」，借这个契机把各通道与平台能力都实测/查证了一遍。

**先纠正一个直觉**：实测「开 VPN」反而更慢 —— VPN 会把**所有**流量（含国内快通道）绕道境外出口：

| 时间 | 梯子 | CDN（jsDelivr） | 官方 GitHub | ghfast 镜像 |
|---|---|---|---|---|
| 约 23:55 | 关 | **316 KB/s** | — | 378 KB/s |
| 00:18 | 开 | **0 KB/s** | 1 KB/s | 12 KB/s |

**平台能力边界（查证官方文档）**：
- ❌ App 无法自行开启 VPN（`VpnService.prepare()` 必须用户手动确认）
- ❌ `DownloadManager` 没有任何代理设置 API（它跑在系统 `com.android.providers.downloads` 进程）
- ⚠️ **per-app VPN（分应用代理）下，DownloadManager 的下载进程可能根本没走你的 VPN**
  —— 这能解释「App 请求正常、下载却龟速」
- ✅ 可用 `NetworkCapabilities.TRANSPORT_VPN` 检测本 App 流量是否真的走 VPN

**本版改动：并发测速选源**
- 点「立即更新」后先并发探测各源（每源最多读 256KB / 2.5 秒，代价约 2.5 秒）
- 选**当时最快**的源再交给 DownloadManager 下载；全部不达标则按默认顺序尝试
- 对话框内展示各源实测速度，例如 `测速：CDN 320 KB/s ｜ 镜像 8 KB/s ｜ 官方 失败`
- 「重试」会重新测速（网络状况可能已变）；下载中的看门狗（停滞/过慢自动换源）继续生效
- 实测 jsDelivr 支持 Range 请求（返回 206），探测不会浪费整包流量

**给用户的建议（比改 App 更有效）**：把 `cdn.jsdelivr.net`、`ghfast.top` 加入梯子的
**直连（DIRECT）规则** —— 规则/分流模式下它们若被兜底规则走了代理，就会如上表那样掉到 0。

测试：新增 `UpdateSourceProbeTest`（8 例，覆盖选优/阈值/失败排除/摘要格式）；**88 个单测全绿**。

## v1.2.5 — 新增 CDN 下载通道与自动换源

下载慢的实测结论：同一时刻各通道速度差异极大且**随时间剧烈波动**（同一镜像一小时从 378 KB/s 掉到 8 KB/s）。

- **新增 CDN 通道**：APK 放到仓库 `dist` 分支由 jsDelivr 分发（路径带版本号 → 永久缓存且不会返回旧版本）；
  `version.json` 新增 `apkUrlCdn` 字段，候选顺序改为 **CDN → 镜像 → 官方**
- **看门狗自动换源**：停滞 8 秒、或开始 12 秒后速度仍低于 15 KB/s → 取消并自动切下一个源
- 下载中显示**实时速度**与已下载/总大小，并可手动「换源」
- **修复进度条视觉**：原 Material3 `LinearProgressIndicator` 在 0% 时会在轨道**右端**绘制
  「停止指示点」，看起来像进度从右往左 → 改为自绘进度条，严格左→右填充

## v1.2.4 — 修复教务导入被验证码误拦

用户实测反馈：填好学号密码点登录，直接被告知「需要验证码，当前版本不支持」。

**根因是我的误报**。抓取学校 CAS 登录页与其 `login.js` 后发现：
- 登录页 HTML 里**始终**存在验证码标记（隐藏的 `#captchaDiv`、`reloadCaptcha()` 等），
  我据此直接阻断 —— 但验证码其实是**按需出现**的
- 官方前端逻辑是：先请求 `/checkNeedCaptcha.htl?username=<学号>` 拿 `{"isNeed":…}`，
  再由返回结果决定是否显示验证码（连续输错才会变成需要）
- 用该学号实测该接口返回 **`{"isNeed":false}`** —— 也就是**本账号根本不需要验证码**，
  原来的实现把可正常登录的路径堵死了

修复后的流程：

1. 打开登录页 → 调 `checkNeedCaptcha.htl` 询问该账号是否需要验证码
2. **不需要** → 直接登录（不再打扰用户）
3. **需要** → 从 `getCaptcha.htl` 取图片显示，用户输入后提交；
   点图片或「换一张」可刷新
4. 登录失败按返回页面的错误文案区分：
   - 「密码错误」→ 提示账号密码问题，**停止重试**（避免多次失败触发风控）
   - 「验证码错误/已失效」→ 自动换一张新验证码（`execution` 是一次性令牌，会重新取登录页）

其他改进：
- 登录页缺 `#pwdEncryptSalt` 时使用页面 JS 内置的默认盐（`DEFAULT_SALT`）兜底
- 新增 `core/jw/JwLoginAnalysis`（纯逻辑）+ 10 个单元测试：覆盖 `isNeed` 解析容错、
  失败原因分类、隐藏/可见验证码区块判断
- 单元测试 70 → **80 个**，全部通过

## v1.2.3 — 更新检查多源兜底

发现 `raw.githubusercontent.com` 在国内是**间歇性可达**的（连测三次：200 / 200 / 连不上），
而用来取更新清单的正是这个地址 —— 这解释了「有时能收到更新提示、有时完全没有」。

- **清单地址三级兜底**：原生 raw → jsDelivr CDN → 加速代理，第一个取到内容的即用
- 逻辑上只在「取不到/解析失败」时才换下一个源；一旦拿到有效清单就立刻做版本判断，不做多余请求
- 实测三个源均返回 200；`jsDelivr` 与 `ghfast.top` 不受 raw 抽风影响
- 同时用户开启 VPN 后 GitHub 官方下载链路也恢复（实测可达，但速度仅 ~35 KB/s，
  故下载仍默认镜像优先，见 v1.2.2）
- 单元测试 70 个，全部通过

## v1.2.2 — 下载走镜像源，并加签名校验

背景：实测同一网络下 GitHub Release 直链只有约 35 KB/s（180 秒都没下完 12.6MB），
而国内镜像 `ghfast.top` 达到 **378 KB/s 且 33 秒完整下载**——差 10 倍。同时用户开启了 VPN，
官方域名也确实可达了，于是把「来源选择」做成一等公民。

- **默认优先镜像源**：更新下载默认走 `apkUrlMirror`（不再让用户干等 6 分钟）
- **记住成功来源**：从哪个源下成功过，下次优先用它（`Prefs.updateSourceIndex`）
- **失败可切换来源**：一键在「镜像加速 / GitHub 官方」之间切换重试
- **安装包双重校验**（第三方镜像不可信，必须校验）：
  - **版本校验**：下载包的 versionCode 必须 ≥ 远端声明值 → 挡住「镜像缓存返回旧版本」
  - **签名校验**：下载包的签名必须与当前已安装应用一致 → 挡住被替换的安装包
  - 包名校验 + 有效性校验（排除 HTML 错误页、半截文件）保持不变
- 失败提示补充：若手机已开 VPN，切 GitHub 官方源通常也能通
- 单元测试 68 → **70 个**，全部通过

## v1.2.1 — 修复「点了立即更新没有任何反应」

用户实测反馈：弹窗出现、点「立即更新」后毫无动静，重进 App 仍反复提示，从未真正更新。
排查确认是**四个缺陷叠加**，其中第一个是根因：

1. **下载完成广播收不到（根因）**：`ACTION_DOWNLOAD_COMPLETE` 由跨进程的系统组件发出，
   原实现用 `RECEIVER_NOT_EXPORTED` 注册，Android 13+ 下会**静默失效**——
   注册不报错，但回调永远不来。
   改为**轮询 `DownloadManager.query`**：既绕开广播限制，又能拿到进度与失败原因。
2. **失败被静默吞掉**：原实现收到广播后不检查 `STATUS_SUCCESSFUL/FAILED`，
   文件不存在就直接 `return`。现在每个失败路径都有可读原因（HTTP 错误 / 断网 / 空间不足 / 被拦截）。
3. **点击后弹窗直接关闭**：完全没有进度与反馈。现在有下载进度对话框，
   失败时可「换镜像源重试 / 用浏览器下载 / 去授权安装权限」，也可取消下载。
4. **缺少安装前置条件处理**：未检查「安装未知应用」权限（Android 8+ 必须），
   也没校验下载物是不是真 APK（GitHub 异常时可能返回 HTML 错误页）。
   现在安装前用 `getPackageArchiveInfo` 校验包名，缺权限时引导到设置页。

顺带增强：

- version.json 新增可选字段 **`apkUrlMirror`**（备用下载源）：GitHub 直连不通时一键切换
  （已实测 `ghfast.top` 镜像能返回真实 APK 字节）
- 已下载但没装成的更新包，下次进入可**直接继续安装**，不必重新下载
- 更新清单解析抽到 `core/UpdateManifest`（纯逻辑）+ 7 个单元测试；
  过程中发现并修掉 Gson 反射绕过 Kotlin 默认值导致缺字段 JSON 会崩的隐患
- 单元测试 61 → **68 个**，全部通过

## v1.2.0 — 教务系统直接导入（中南大学）

终于不用手敲课表了：在 App 里输入学号密码，直接从学校教务系统拉课表。

### 新增：教务系统导入

- **登录链路**：中南大学本科教务系统（`csujwc.its.csu.edu.cn`）走统一认证 CAS
  （`ca.csu.edu.cn`），按网页端规则用 **AES-128-CBC** 加密密码后提交；
  由于各校 IV 处理方式不同，客户端会按「iv+密文 → 仅密文 → 明文」三种方案自动重试
- **课表解析**：`POST /jsxsd/xskb/xskb_list.do` 返回的 HTML 表格，自动识别
  表头列偏移与星期顺序（周一/周日在前的两种排版都支持），解析出
  课程名 / 教师 / 星期 / 节次 / 周次 / 地点，并处理同一格多门课
  （`-----`、`<hr>`、多个 `div.kbcontent` 三种写法）、`1-8,10-12(周)` 逗号周次、单双周
- **学期切换**：自动读取教务系统的学期下拉框，可在导入页直接切换学期
- **开学日推算**：教务只给学期编号，按「秋季学期 9 月 1 日 / 春季学期次年 2 月 20 日」
  推算所在周的周一作为开学日，导入后可在课表设置里微调
- **隐私**：学号密码只用于当次登录请求，不写数据库、不写日志（日志只记密码长度）
- **可诊断**：导入页内置「调试日志」，失败时一键复制给开发者即可定位（不含密码）

### 修复

- 课程名末尾的括号不再被误删（`高等数学A(下)` 之前会被截成 `高等数学A`）
- 单元格未标注节次时，不再把 `1-16(周)` 里的数字误当节次（会导致课节错位）
- 强智页面多为 GBK 编码，新增编码嗅探，避免中文课程名乱码
- 新增 network security config：默认禁止明文 HTTP，仅对学校域名放开

### 工程

- 新增 `core/jw/`：`JwClient`（登录/取课表）、`JwScheduleHtmlParser`（HTML 解析）、
  `JwCrypto`（CAS 密码加密）、`JwTermDate`（开学日推算）——全部纯 JVM，零 Android 依赖
- 引入 Jsoup 做 HTML 解析
- **单元测试 37 → 61 个**：新增密码加密 6 + 课表解析 13 + 开学日推算 5

## v1.1.0 — 稳定性与体验提升

这一版由「代码质量提升」专项驱动：独立代码审计 + 逐项修复 + 补测试。**新增 15 个单元测试（共 37 个，全绿）**。

### 修复（数据与稳定性）

- **课程保存改为数据库事务**：此前「更新课程 → 删除旧时间段 → 写入新时间段」是三步独立操作，中途失败会留下课程改了、时间段还是旧的半成品数据
- **删除课表后当前课表偏好未修正**：偏好会指向已删除的 id，导致课表列表高亮错乱、主界面显示错误课表
- **开学日期强制归一到周一**：用户填了周三会导致整个学期周次偏移一周（创建、修改、导入三条路径都做了归一化）
- **编辑课程时无课表不再写入孤儿数据**：没有课表时自动创建默认课表，避免课程挂到不存在的 tableId
- **背景图权限失败不再保存无效 URI**：此前 `takePersistableUriPermission` 失败仍写入 URI，重启后背景必然丢失，现在给出明确提示且不落库

### 修复（性能 / 主线程）

- **主线程阻塞清除**：首启示例课表播种不再使用 `runBlocking`（原实现有 ANR 风险），改为 IO 协程 + 启动加载态
- **整周网格小部件改为异步渲染**：`onUpdate` 里的查库与 Bitmap 绘制移到 IO 线程（`goAsync`），不再阻塞主线程
- **周数滑竿不再拖动即翻页**：改为松手才滚动，消除拖动卡顿

### 新增功能

- **桌面小部件自动刷新**：Repository 暴露数据版本流，任何增删改（课程 / 课表 / 外观）都会立刻重绘两个小部件——此前必须等系统 30 分钟周期
- **课程时间冲突检测**：编辑课程时实时提示「与『数据结构与算法』冲突：周一第 1-2 节，第 1-8 周」，已正确处理起止周与单双周（单周课 × 双周课不算冲突）
- **删除课程可撤销**：删除后 Snackbar 提供「撤销」，一键恢复课程与全部时间段
- **课程搜索**：按课程名 / 教师 / 备注 / 地点过滤
- **完整备份与恢复**：可一次性导出**全部课表**（含各自外观、节次时间），导入时自动识别单课表备份与全量备份，兼容 v1 旧备份
- **更新提示支持「忽略此版本」**：不再每次启动都打扰

### 工程

- 备份 JSON 编解码抽到 `core/BackupCodec`（纯 JVM），新增 6 个测试覆盖往返、旧版本兼容、脏数据兜底
- 冲突检测抽为纯逻辑 `ScheduleMath.SlotKey / overlappingWeeks / conflicts`，新增 9 个测试覆盖边界
- 新增 `docs/ROADMAP.md`：质量基线、已知取舍、后续优先级、本机环境备忘
- 版本号 → `1.1.0`（versionCode 2）

## v1.0.0 — 首个版本

- 周视图课表网格、左右翻页切周、非本周一键回跳、日期栏今日高亮
- 课程增删改查（多时间段、单双周、起止周、连上节数）
- 多课表管理、上课时间设置、课表设置、全局设置（深色模式）
- 课表外观：背景（默认 / 纯色 / 相册图片）、课程块透明度、显示或隐藏周末
- 桌面小部件 ×2：今日课程列表、整周网格（Canvas 绘制）
- 导入导出：JSON 备份、分享口令（`WKUP:` + Base64）、Excel/CSV 模板
- 应用内更新：启动检查 `version.json` + DownloadManager 下载安装
- 内置示例课表，首次启动即可看到完整效果
