# 更新日志

## v1.2.0 — 教务系统直接导入（中南大学）

终于不用手敲课表了：在 App 里输入学号密码，直接从学校教务系统拉课表。

### 新增：教务系统导入

- **登录链路**：中南大学本科教务系统（`csujwc.its.csu.edu.cn`）走统一认证 CAS
  （`ca.csu.edu.cn`），按网页端规则用 **AES-128-CBC** 加密密码后提交；
  由于各校 IV 处理方式不同，客户端会按「iv+密文 → 仅密文 → 明文」三种方案自动重试
- **课表解析**：`POST /jsxsd/xskb/xskb_list.do` 返回的 HTML 表格，自动识别
  表头列偏移与星期顺序（周一/周日在前的两种排版都支持），解析出
  课程名 / 教师 / 星期 / 节次 / 周次 / 地点，并处理同一格多门课
  （`-----`、`<hr>`、多个 `div.kbcontent` 三种写法）、`1-8,10-12(周)` 逗号周次、单双周
- **学期切换**：自动读取教务系统的学期下拉框，可在导入页直接切换学期
- **开学日推算**：教务只给学期编号，按「秋季学期 9 月 1 日 / 春季学期次年 2 月 20 日」
  推算所在周的周一作为开学日，导入后可在课表设置里微调
- **隐私**：学号密码只用于当次登录请求，不写数据库、不写日志（日志只记密码长度）
- **可诊断**：导入页内置「调试日志」，失败时一键复制给开发者即可定位（不含密码）

### 修复

- 课程名末尾的括号不再被误删（`高等数学A(下)` 之前会被截成 `高等数学A`）
- 单元格未标注节次时，不再把 `1-16(周)` 里的数字误当节次（会导致课节错位）
- 强智页面多为 GBK 编码，新增编码嗅探，避免中文课程名乱码
- 新增 network security config：默认禁止明文 HTTP，仅对学校域名放开

### 工程

- 新增 `core/jw/`：`JwClient`（登录/取课表）、`JwScheduleHtmlParser`（HTML 解析）、
  `JwCrypto`（CAS 密码加密）、`JwTermDate`（开学日推算）——全部纯 JVM，零 Android 依赖
- 引入 Jsoup 做 HTML 解析
- **单元测试 37 → 61 个**：新增密码加密 6 + 课表解析 13 + 开学日推算 5

## v1.1.0 — 稳定性与体验提升

这一版由「代码质量提升」专项驱动：独立代码审计 + 逐项修复 + 补测试。**新增 15 个单元测试（共 37 个，全绿）**。

### 修复（数据与稳定性）

- **课程保存改为数据库事务**：此前「更新课程 → 删除旧时间段 → 写入新时间段」是三步独立操作，中途失败会留下课程改了、时间段还是旧的半成品数据
- **删除课表后当前课表偏好未修正**：偏好会指向已删除的 id，导致课表列表高亮错乱、主界面显示错误课表
- **开学日期强制归一到周一**：用户填了周三会导致整个学期周次偏移一周（创建、修改、导入三条路径都做了归一化）
- **编辑课程时无课表不再写入孤儿数据**：没有课表时自动创建默认课表，避免课程挂到不存在的 tableId
- **背景图权限失败不再保存无效 URI**：此前 `takePersistableUriPermission` 失败仍写入 URI，重启后背景必然丢失，现在给出明确提示且不落库

### 修复（性能 / 主线程）

- **主线程阻塞清除**：首启示例课表播种不再使用 `runBlocking`（原实现有 ANR 风险），改为 IO 协程 + 启动加载态
- **整周网格小部件改为异步渲染**：`onUpdate` 里的查库与 Bitmap 绘制移到 IO 线程（`goAsync`），不再阻塞主线程
- **周数滑竿不再拖动即翻页**：改为松手才滚动，消除拖动卡顿

### 新增功能

- **桌面小部件自动刷新**：Repository 暴露数据版本流，任何增删改（课程 / 课表 / 外观）都会立刻重绘两个小部件——此前必须等系统 30 分钟周期
- **课程时间冲突检测**：编辑课程时实时提示「与『数据结构与算法』冲突：周一第 1-2 节，第 1-8 周」，已正确处理起止周与单双周（单周课 × 双周课不算冲突）
- **删除课程可撤销**：删除后 Snackbar 提供「撤销」，一键恢复课程与全部时间段
- **课程搜索**：按课程名 / 教师 / 备注 / 地点过滤
- **完整备份与恢复**：可一次性导出**全部课表**（含各自外观、节次时间），导入时自动识别单课表备份与全量备份，兼容 v1 旧备份
- **更新提示支持「忽略此版本」**：不再每次启动都打扰

### 工程

- 备份 JSON 编解码抽到 `core/BackupCodec`（纯 JVM），新增 6 个测试覆盖往返、旧版本兼容、脏数据兜底
- 冲突检测抽为纯逻辑 `ScheduleMath.SlotKey / overlappingWeeks / conflicts`，新增 9 个测试覆盖边界
- 新增 `docs/ROADMAP.md`：质量基线、已知取舍、后续优先级、本机环境备忘
- 版本号 → `1.1.0`（versionCode 2）

## v1.0.0 — 首个版本

- 周视图课表网格、左右翻页切周、非本周一键回跳、日期栏今日高亮
- 课程增删改查（多时间段、单双周、起止周、连上节数）
- 多课表管理、上课时间设置、课表设置、全局设置（深色模式）
- 课表外观：背景（默认 / 纯色 / 相册图片）、课程块透明度、显示或隐藏周末
- 桌面小部件 ×2：今日课程列表、整周网格（Canvas 绘制）
- 导入导出：JSON 备份、分享口令（`WKUP:` + Base64）、Excel/CSV 模板
- 应用内更新：启动检查 `version.json` + DownloadManager 下载安装
- 内置示例课表，首次启动即可看到完整效果
